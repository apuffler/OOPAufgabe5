public interface Smaller<T>{

	/**
     * Interface for smaller which returns true if "this" festmeter is smaller than the parameters festmeter. 
     * @param (x): Must NOT be NULL!
     **/ 
    public boolean smaller(T x);
}
